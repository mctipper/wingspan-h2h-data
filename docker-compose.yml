services:
  public:
    build: .
    command: npm run dev
    ports:
      - "5173:5173"
    restart: unless-stopped

  admin:
    build: .
    command: npm run dev:admin
    ports:
      - "5173:5174"
    restart: unless-stopped
