services:
  public:
    build: .
    command: npm run dev
    volumes:
     - /home/mog/wingspan-h2h-data:/app
     - /app/node_modules
    ports:
      - "5173:5173"
    restart: unless-stopped

  admin:
    build: .
    command: npm run dev:admin
    volumes:
     - /home/mog/wingspan-h2h-data:/app
     - /app/node_modules
    ports:
      - "5174:5173"
    restart: unless-stopped
